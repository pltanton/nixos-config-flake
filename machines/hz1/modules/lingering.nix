{ pkgs, ... }:
{
  # config.system.activationScripts = {
  #   enableLingering = ''
  #     # remove all existing lingering users
  #     rm -r /var/lib/systemd/linger
  #     mkdir /var/lib/systemd/linger
  #     # enable for the subset of declared users
  #     touch /var/lib/systemd/linger/deploy
  #   '';
  # };

}
